<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | PurrBite</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">
</head>
<body>

    <div class="login-bg">
      <div class="login-container">
        <h2 class="login-title">Login</h2>
        <form action="/login" method="POST" class="login-form">
          <%- include('../partials/errors') %>
          <div class="login-field">
            <input type="email" placeholder="Email" name="email" required value="<%= oldInput.email ? oldInput.email : '' %>" />
          </div>
          <div class="login-field">
            <input type="password" placeholder="Password" name="password" required value="<%= oldInput.password ? oldInput.password : '' %>" />
          </div>
          <button class="login-btn" type="submit">Login</button>
        </form>
      </div>
    </div>
    <style>
      body {
        font-family: 'Montserrat', Arial, sans-serif;
        background: linear-gradient(120deg, #7f53ac 0%, #657ced 100%);
        min-height: 100vh;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .login-bg {
        width: 100vw;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(120deg, #7f53ac 0%, #657ced 100%);
      }
      .login-container {
        background: #fff;
        border-radius: 22px;
        box-shadow: 0 4px 32px #7f53ac33;
        padding: 38px 32px 32px 32px;
        max-width: 400px;
        width: 98vw;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      .login-title {
        font-size: 2.1rem;
        font-weight: 600;
        color: #7f53ac;
        margin-bottom: 18px;
        letter-spacing: 1px;
        text-shadow: 0 2px 8px #7f53ac22;
      }
      .login-form {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 18px;
      }
      .login-field {
        width: 100%;
        display: flex;
        flex-direction: column;
      }
      .login-field input {
        padding: 12px 14px;
        border-radius: 12px;
        border: 2px solid #7f53ac;
        font-size: 1rem;
        font-family: inherit;
        background: #f8f8ff;
        margin-bottom: 2px;
        transition: border-color 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 8px #7f53ac11;
      }
      .login-field input:focus {
        border-color: #657ced;
        outline: none;
        box-shadow: 0 4px 16px #657ced22;
      }
      .login-btn {
        width: 100%;
        padding: 12px 0;
        background: linear-gradient(90deg, #7f53ac 0%, #657ced 100%);
        color: #fff;
        font-size: 1.15rem;
        font-weight: 600;
        border: none;
        border-radius: 14px;
        box-shadow: 0 2px 12px #7f53ac22;
        cursor: pointer;
        transition: background 0.2s, box-shadow 0.2s;
      }
      .login-btn:hover {
        background: linear-gradient(90deg, #657ced 0%, #7f53ac 100%);
        box-shadow: 0 6px 24px #7f53ac44;
      }
      @media (max-width: 600px) {
        .login-container {
          padding: 18px 6vw 18px 6vw;
          max-width: 98vw;
        }
        .login-title {
          font-size: 1.3rem;
        }
        .login-btn {
          font-size: 1rem;
          padding: 10px 0;
        }
      }
    </style>


</body>
</html>